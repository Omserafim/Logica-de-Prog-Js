const usuario = {
    email: "omserafim@gmail.com"
}

console.log(usuario.email)
console.log(usuario.nome)

usuario.nome = "Otávio"

console.log(usuario.email)
console.log(usuario.nome)

// Perceba que eu posso criar os atributos de forma dinâmica, não necessariamente eu preciso definir tudo diretamente no objeto.
