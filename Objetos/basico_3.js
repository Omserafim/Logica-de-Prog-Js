const data = {
    dia: 1,
    mes: 2,
    ano: 2023
}

// Como mostrar todos os valores?
console.log(`${data.dia}/${data.mes}/${data.ano}`)

// Como alterar os valores dentro do objeto? 

data.dia = 23

console.log(`${data.dia}/${data.mes}/${data.ano}`)